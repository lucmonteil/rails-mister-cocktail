_<div class="card">
  <div class="card-header">
    <%= image_tag 'http://lorempixel.com/300/200/food' %>
  </div>
  <div class="card-body">
    <h2><%= ingredient.name %></h2>
    <p><%= link_to 'Show', ingredient, class: "btn btn-primary"%></p>
    <p><%= link_to 'Edit', edit_ingredient_path(ingredient), class: "btn btn-primary" %></p>
    <p><%= link_to 'Destroy', ingredient, method: :delete, data: { confirm: 'Are you sure?' }, class: "btn btn-primary" %></p>
  </div>
</div>
